<%= form_for([@article, @article.comments.build]) do |f| %>
  <p>
    <%= f.label :commenter,"评论者" %><br>
    <%= f.text_field :commenter %>
  </p>
  <p>
    <%= f.label :body,"评论" %><br>
    <%= f.text_area :body %>
  </p>
  <p>
    <%= f.submit "发表评论"%>
  </p>
<% end %>